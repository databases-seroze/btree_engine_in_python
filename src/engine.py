"""
The engine is the main entry point for the database. It manages the database file, the B+ tree structure, and provides methods for
inserting, deleting, and querying data.

Responsibilities:
    - put(key, value)
    - get(key)
    - delete(key)
"""

